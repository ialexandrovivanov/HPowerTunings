@using HPowerTunings.ViewModels.Appointment
@model ProceedAppointmentModel

@{
    ViewData["Title"] = "ProceedAppointment";
    Layout = "~/Views/Shared/_LayoutBoss.cshtml";
}
<style>
    body {
        background-color: rgb(234, 238, 238)
    }
</style>

<body>
    <div class="container-fluid justify-content-center mt-5 pt-5" style="max-width:480px">
        <h3 class="text-center">Set final <strong>date/time</strong> for this appointment</h3>
        <form class="d-flex" id="finalize" method="post">
            <div class="form-group mt-4">
                <label class="container-fluid justify-content-center text-center">Set appointment date</label>
                <input type="datetime-local" asp-for="@Model.AppointmentDate" class="form-control"
                       style="max-width:350px" />
                <input type="hidden" asp-for="@Model.In.Id" class="form-control disabled" value="@Model.In.Id"/>
                <input type="hidden" asp-for="@Model.In.ClientEmail" class="form-control disabled" value="@Model.In.ClientEmail"/>
                <span asp-validation-for="@Model.AppointmentDate" class="text-danger"></span>
            </div>
            <div>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;</div>
            <div class="button-holder justify-content-center" style="margin-top:56px">
                <button type="submit" class="btn btn-outline-success" style="opacity:0.9">Finalize</button>
            </div>
        </form>

        <div style="border:solid rgb(220, 220, 220) 1px; border-radius:6px; padding:20px; background-color:aliceblue ">
            <p>Client username: <strong style="color:indigo">@Model.In.ClientUserName</strong></p>
            <p>Client phone number: <strong style="color:indigo">@Model.In.ClientPhoneNumber</strong></p>
            <p>Client email: <strong style="color:indigo">@Model.In.ClientEmail</strong></p>
            <p>Desired date for appoinment: <strong style="color:indigo">@Model.In.AppointmentDate</strong></p>
            <p>Car brand: <strong style="color:indigo">@Model.In.CarBrandName</strong></p>
            <p>Car model: <strong style="color:indigo">@Model.In.CarModelName</strong></p>
            <p>Problem description: <strong style="color:indigo">@Model.In.ProblemDescription</strong></p>
        </div>
    </div>
    <div style="height:80px"></div>
</body>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
